# .circleci/config.yml
defaults: &defaults
  docker:
    - image: python:3.8
  working_directory: ~/project
  
version: 2.1
jobs:
    build:
      <<: *defaults
      steps:
        - checkout  # checkout source code to working directory
        - run:
            command: |
              python -m venv env
              source env/bin/activate
              python -m pip install --upgrade setuptools
              python -m pip install wheel pytest pytest-cov
              python setup.py install
              pycr -h
              pytest .
        - save_cache:
             key: "py-dependencies"
             paths: 
                - /root/project/env/lib/python3.8/site-packages
                - /root/project/env/bin
    test:          
      <<: *defaults
      steps:
        - checkout  # checkout source code to working directory
        - restore_cache:
             keys:
               - "py-dependencies"
        - run:
            command: |
              source env/bin/activate
              pytest .
workflows:
  integration:
    jobs:
      - build
      - test:
          requires:
              - build
